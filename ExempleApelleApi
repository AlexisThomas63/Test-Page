let users = null;

function init() {
    chargerUsers();
}

function chargerUsers() {
    fetch("/api/users")
        .then(r => r.json())
        .then(data => {
            users = data;
            render();
        });
}

function render() {
    if (!users) return;

    console.log(users);
}
